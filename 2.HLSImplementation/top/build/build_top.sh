#!/bin/sh
# Automatically generated by Amaranth 0.5.1.dev3. Do not edit.
set -e
if [ -z "$BASH" ] ; then exec /bin/bash "$0" "$@"; fi
if [ -n "$AMARANTH_ENV_DIAMOND" ]; then
    bindir=$(dirname "$AMARANTH_ENV_DIAMOND")
    . "$AMARANTH_ENV_DIAMOND"
fi
: ${PNMAINC:=pnmainc}
: ${DDTCMD:=ddtcmd}
: ${OPENFPGALOADER:=openFPGALoader}
"$PNMAINC" top.tcl
"$DDTCMD" -oft -bit -if top_impl/top_impl.bit -of top.bit
"$DDTCMD" -oft -svfsingle -revd -op "Fast Program" -if top_impl/top_impl.bit -of top.svf
